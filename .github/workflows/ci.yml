name: CI Pipeline (Pruebas y Verificación)

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  test-backend:
    name: Test & Lint Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Instalar dependencias del Backend
        run: |
          cd apps/api
          pnpm install

      - name: Generar cliente de Prisma
        run: |
          cd apps/api
          pnpm run db:generate

      - name: Ejecutar Tests de Vitest
        env:
          NODE_ENV: test
          JWT_SECRET: 1f7f73e124735f955ecdde8f6605e0eb4ec3b4dadfe1308089704d6befdbad1a
        run: |
          cd apps/api
          pnpm run test

  deploy:
    name: Deploy to Production
    needs: test-backend 
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: Disparar Deploy en Vercel (Frontend)
        run: |
          curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"

      - name: Disparar Deploy en Render (API)
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"